[project]
name = "dkf"
version = "0.1.0"
dependencies = ["requests>=2.31", "PyYAML>=6.0"]

[project.scripts]
dkf = "dkf.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
