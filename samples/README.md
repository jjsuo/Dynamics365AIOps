# samples — 离线验证数据集

这套 mock 元数据模拟一个 D365 售后/合同场景（合同、服务工单、备件派工、客户主数据），
格式与 DKF `scan` 命令真实产出一致。用于**零云端**验证框架可用性。

- `meta/`            模拟 K01 扫描产物（实体/入口/流程/安全/...）
- `K03_confirmed.json` 模拟 K02 划分 + K03 人工确认后的领域

跑 `bash validate.sh`（项目根目录）即可看到 domains/k04/index 的完整产出。
