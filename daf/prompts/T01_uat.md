# T01 — 端到端 / UAT prompt（飞书异步放行）

## 任务
按需求的 acceptance_criteria（来自 P01）逐条设计端到端业务流程测试，在 UAT 环境执行：
1. 把每条验收标准转成可执行测试步骤
2. 跨模块链路引用 .dkf/cross/ 的链路图，确保不漏跨域场景
3. 产出测试报告 + 缺陷清单；缺陷回流到对应模块话题

## 放行
UAT 通过后 `notify_feishu.py --stage T01` 发验收卡片，业务方在话题里签核。
